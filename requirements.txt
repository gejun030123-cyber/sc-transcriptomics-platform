# Core web/runtime
flask
flask-cors
psutil
requests

# Numeric/data stack
numpy
pandas
scipy
statsmodels
patsy
scikit-learn
matplotlib
adjustText
Pillow
plotly
anndata
# Excel input/output is advertised by the upload and result interfaces; pandas
# intentionally treats this engine as optional, so declare it explicitly.
openpyxl
# Used directly to compare installed pyscdblfinder versions in the QC gate.
packaging

# Single-cell analysis
scanpy
# Functional-state TF activity (CollecTRI + ULM). Pin the API used by the
# managed resource workflow; each analysis also records the installed version.
decoupler==2.2.0
omicverse
# Leiden/Louvain 聚类的必需后端（scanpy 仅将其列为可选 extra，
# 未安装时默认聚类路径会 ImportError）
igraph
leidenalg
louvain
# qc 双细胞检测
scrublet
pyscdblfinder>=0.2.0

# Bulk RNA-seq and enrichment
gseapy
pydeseq2
inmoose

# Optional WES content-level checks (BAM/CRAM/FASTA/VCF headers and indices).
# Variant calling and VCF normalization remain external tools (GATK,
# samtools/bcftools) executed by the configured Nextflow profile.
pysam

# Optional single-cell annotation / communication / integration
celltypist
liana
harmonypy
bbknn
scanorama
scvi-tools
torch
kneed
kaleido
# Used by the 2–3 comparison DEG-overlap panel.  Keep it in the baseline so a
# fresh clone does not silently skip the Venn artifact.
matplotlib-venn

# Optional AI providers
openai
anthropic
