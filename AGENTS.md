# 项目开发规则

## 使用场景

本平台面向实验室内部科研人员，由小型团队维护，默认运行在受控内网或实验室服务器上。首期按少量并发任务（通常 1–2 个重计算任务）设计，不以医院级、SaaS 多租户或大规模 HPC 调度平台为默认目标。

## 复杂度原则

1. 优先增量复用现有 Flask、SQLite、项目目录、后台任务和模块注册能力，不为潜在需求提前重构整个平台。
2. 选择能满足当前实验室使用规模的最简单可靠实现；只有出现明确扩展触发条件时才引入复杂架构。
3. 规划和实施任务时将内容分成“当前必需、可选增强、明确延期”，不得把企业级能力默认列为首期阻断项。
4. WES 等长任务首期使用 Nextflow local executor（默认 Docker profile）与固定容器环境；先支持启动、查询、取消和简单 `-resume`，不预先建设通用调度中心。
5. SQLite 首期继续使用轻量、可重复的 schema upgrade；除非出现多实例写入或明显并发瓶颈，不迁移到复杂数据库栈。
6. 只实现当前科研流程需要的数据模型。运行尝试历史、完整事件溯源、复杂幂等和多版本 reference catalog 可在出现实际需求后增加。
7. 新能力不得破坏现有单细胞和 Bulk RNA-seq 行为，优先通过兼容层或独立 workflow registry 接入。
8. WES 胚系与肿瘤分析只共用输入登记、比对、基础 QC 等公共阶段；caller、样本配对、过滤资源、结果字段和报告必须使用独立 workflow contract。配对正常样本可分别参与胚系和 somatic run，但两类变异结果不得混表或共用过滤结论。

## 不得因“内部使用”省略的底线

1. 人类基因组数据仍属于敏感数据：必须限制服务器路径、拒绝符号链接和目录穿越，并避免向外部 AI 发送 FASTQ、BAM、CRAM、全量 VCF、绝对路径或直接身份信息。
2. 外部命令必须由服务端白名单和固定参数模板生成，使用参数数组与 `shell=False`；网页和 AI 不得提供任意命令。
3. Workflow、容器、reference bundle、capture BED、参数和输入 checksum 必须可追溯且不得在单次运行中混用版本。
4. 长任务必须保存状态、PID/进程组、stdout/stderr 和退出码；取消操作必须真正终止子进程。
5. 科研结论必须保留 QC、过滤状态和适用边界，不得把自动注释表述为临床诊断。
6. 真实人类数据投入使用前，至少具备登录或受控内网、项目访问限制、备份和数据保留策略。

## 升级触发条件

只有出现以下一种或多种实际情况时，才评估引入 PostgreSQL、任务队列、完整 migration 框架、运行 attempt/event 模型、Slurm/Kubernetes 或细粒度 RBAC：

- 同时运行的重计算任务稳定超过 2 个；
- 多个 Web/worker 实例需要并发写入；
- 服务重启后必须自动恢复大量运行任务；
- 开放给实验室外部人员或多个相互隔离的团队；
- 进入临床、合规审计或正式多租户场景；
- 实际故障证明当前轻量实现无法可靠维护。
