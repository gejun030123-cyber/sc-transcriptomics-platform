# 10x Genomics 三文件转 h5ad 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在现有上传页面增加 10x Genomics 三文件（barcodes.tsv、genes.tsv/features.tsv、matrix.mtx）自动识别和一键转换为 h5ad 的功能。

**Architecture:** 复用现有上传页面，后端新增 `convert_10x_to_h5ad()` 函数（基于 scanpy）和 `Convert10x` 任务模块，前端通过 JS 自动检测上传文件组合并显示转换界面。转换通过现有的 `AnalysisTask` + `ThreadPoolExecutor` 异步执行。

**Tech Stack:** Python, Flask, scanpy (`read_10x_mtx`), Jinja2, Bootstrap 5, 原生 JavaScript (XHR)

**设计文档:** `docs/superpowers/specs/2026-06-05-10x-to-h5ad-design.md`

---

## 文件结构

| 文件 | 操作 | 职责 |
|------|------|------|
| `modules/io_utils.py` | 修改 | 新增 `convert_10x_to_h5ad()` 函数 |
| `modules/convert_10x.py` | 新建 | Convert10x 任务模块，继承 BaseAnalysis |
| `modules/__init__.py` | 修改 | 注册 convert_10x 到 MODULE_REGISTRY |
| `routes/upload.py` | 修改 | 新增 check-10x 和 convert-10x API 端点 |
| `templates/upload.html` | 修改 | 新增 10x 检测区域、转换表单、多文件上传、进度轮询 |

---

## Task 1: 后端转换函数

**Files:**
- Modify: `modules/io_utils.py`

- [ ] **Step 1: 在 `modules/io_utils.py` 末尾添加 `convert_10x_to_h5ad()` 函数**

在文件末尾（`remap_var_names` 函数之后）添加：

```python
def convert_10x_to_h5ad(mtx_dir, output_path, species=None, genome=None):
    """
    将 10x Genomics 三文件格式转换为 h5ad。
    自动检测 v2 (genes.tsv) 和 v3 (features.tsv) 格式。

    参数:
        mtx_dir: 包含 barcodes/genes/features/matrix 文件的目录
        output_path: h5ad 输出路径
        species: 可选，物种名（如 "human"、"mouse"）
        genome: 可选，基因组版本（如 "GRCh38"、"mm10"）
    返回:
        anndata.AnnData 对象
    """
    import scanpy as sc

    adata = sc.read_10x_mtx(mtx_dir, var_names='gene_symbols', cache=True)
    adata.var_names_make_unique()

    # 保留 Ensembl ID（read_10x_mtx 在 var_names='gene_symbols' 时
    # 将原始 ID 存为 adata.var 的 gene_ids 列）
    if 'gene_ids' not in adata.var.columns:
        adata.var['gene_ids'] = adata.var.index.tolist()

    # 可选元数据
    if species:
        adata.uns['species'] = species
    if genome:
        adata.uns['genome'] = genome

    # 保存原始计数
    adata.layers['counts'] = adata.X.copy()

    adata.write_h5ad(output_path)
    return adata
```

- [ ] **Step 2: 验证函数语法正确**

```bash
cd /data/GJ/platform && python -c "from modules.io_utils import convert_10x_to_h5ad; print('import OK')"
```

Expected: `import OK`

- [ ] **Step 3: Commit**

```bash
git add modules/io_utils.py
git commit -m "feat: add convert_10x_to_h5ad function to io_utils"
```

---

## Task 2: 转换任务模块

**Files:**
- Create: `modules/convert_10x.py`

- [ ] **Step 1: 创建 `modules/convert_10x.py`**

```python
import os
from .base import BaseAnalysis
from .io_utils import convert_10x_to_h5ad


class Convert10x(BaseAnalysis):
    MODULE_NAME = "convert_10x"
    DISPLAY_NAME = "10x 数据转换"
    DESCRIPTION = "将 10x Genomics 三文件格式转换为 h5ad"

    def validate_input(self, adata):
        return None  # 不需要输入 adata

    def run(self, input_path):
        mtx_dir = self.params['mtx_dir']
        species = self.params.get('species')
        genome = self.params.get('genome')

        self.progress(10, '正在读取 10x 数据...')
        output_path = os.path.join(self.project_dir, 'uploads', 'converted_10x.h5ad')

        self.progress(30, '正在解析矩阵文件...')
        adata = convert_10x_to_h5ad(mtx_dir, output_path, species=species, genome=genome)

        self.progress(90, f'转换完成，共 {adata.n_obs} 个细胞，{adata.n_vars} 个基因')

        return {
            'output_adata': output_path,
            'result_files': [],
            'summary': {
                'n_cells': adata.n_obs,
                'n_genes': adata.n_vars,
                'output_file': os.path.basename(output_path),
            }
        }
```

- [ ] **Step 2: 验证模块可导入**

```bash
cd /data/GJ/platform && python -c "from modules.convert_10x import Convert10x; print(Convert10x.MODULE_NAME)"
```

Expected: `convert_10x`

- [ ] **Step 3: Commit**

```bash
git add modules/convert_10x.py
git commit -m "feat: add Convert10x task module"
```

---

## Task 3: 注册模块

**Files:**
- Modify: `modules/__init__.py`

- [ ] **Step 1: 在 `modules/__init__.py` 中添加 import 和注册**

在 import 区域末尾（第 17 行之后）添加：

```python
from .convert_10x import Convert10x
```

在 `MODULE_REGISTRY` 字典末尾（第 38 行之后）添加：

```python
    # 数据导入模块
    'convert_10x': Convert10x,
```

**注意：不加入 `PIPELINE_ORDER`**，因为 convert_10x 是数据导入，不是分析管线步骤。

- [ ] **Step 2: 验证注册正确**

```bash
cd /data/GJ/platform && python -c "from modules import MODULE_REGISTRY; print('convert_10x' in MODULE_REGISTRY)"
```

Expected: `True`

- [ ] **Step 3: Commit**

```bash
git add modules/__init__.py
git commit -m "feat: register convert_10x module in MODULE_REGISTRY"
```

---

## Task 4: API 端点

**Files:**
- Modify: `routes/upload.py`

- [ ] **Step 1: 在 `routes/upload.py` 中添加 `check_10x_files()` 辅助函数**

在 `ALLOWED_EXT` 定义之后、`upload` 路由之前添加：

```python
# 10x 文件名匹配模式
_10X_FILE_PATTERNS = {
    'barcodes': ['barcodes.tsv', 'barcodes.tsv.gz'],
    'genes_v2': ['genes.tsv', 'genes.tsv.gz'],
    'genes_v3': ['features.tsv', 'features.tsv.gz'],
    'matrix': ['matrix.mtx', 'matrix.mtx.gz'],
}

def check_10x_files(uploads_dir):
    """检测 uploads 目录中是否包含完整的 10x 三文件组合。"""
    if not os.path.isdir(uploads_dir):
        return {'has_10x': False}

    existing = set(os.listdir(uploads_dir))
    result = {'has_10x': False, 'files': {}, 'version': None}

    # 检测 barcodes
    barcodes = None
    for name in _10X_FILE_PATTERNS['barcodes']:
        if name in existing:
            barcodes = name
            break
    if not barcodes:
        return result

    # 检测 matrix
    matrix = None
    for name in _10X_FILE_PATTERNS['matrix']:
        if name in existing:
            matrix = name
            break
    if not matrix:
        return result

    # 检测 genes（优先 v3）
    genes = None
    version = None
    for name in _10X_FILE_PATTERNS['genes_v3']:
        if name in existing:
            genes = name
            version = 'v3'
            break
    if not genes:
        for name in _10X_FILE_PATTERNS['genes_v2']:
            if name in existing:
                genes = name
                version = 'v2'
                break
    if not genes:
        return result

    return {
        'has_10x': True,
        'files': {'barcodes': barcodes, 'genes': genes, 'matrix': matrix},
        'version': version,
    }
```

- [ ] **Step 2: 添加 `GET /projects/<pid>/upload/check-10x` 端点**

在 `upload.py` 文件末尾添加：

```python
@upload_bp.route('/<pid>/upload/check-10x')
def check_10x(pid):
    p = Project.get_by_id(pid)
    if not p:
        return jsonify({'has_10x': False, 'error': '项目未找到'}), 404
    uploads_dir = os.path.join(Config.DATA_DIR, 'projects', pid, 'uploads')
    return jsonify(check_10x_files(uploads_dir))
```

- [ ] **Step 3: 添加 `POST /projects/<pid>/upload/convert-10x` 端点**

在同一文件末尾继续添加：

```python
@upload_bp.route('/<pid>/upload/convert-10x', methods=['POST'])
def convert_10x(pid):
    import json as _json
    from models import AnalysisTask
    from worker import submit_task

    p = Project.get_by_id(pid)
    if not p:
        return jsonify({'error': '项目未找到'}), 404

    uploads_dir = os.path.join(Config.DATA_DIR, 'projects', pid, 'uploads')
    check = check_10x_files(uploads_dir)
    if not check['has_10x']:
        return jsonify({'error': '未检测到完整的 10x 数据文件'}), 400

    species = request.form.get('species', '').strip() or None
    genome = request.form.get('genome', '').strip() or None

    task = AnalysisTask(
        project_id=pid,
        module_name='convert_10x',
        status='pending',
        params_json=_json.dumps({
            'mtx_dir': uploads_dir,
            'species': species,
            'genome': genome,
        }, ensure_ascii=False),
    )
    task.save()

    proj_dir = os.path.join(Config.DATA_DIR, 'projects', pid)
    submit_task(task.id, pid, 'convert_10x',
                {'mtx_dir': uploads_dir, 'species': species, 'genome': genome},
                proj_dir, uploads_dir)

    return jsonify({'task_id': task.id})
```

- [ ] **Step 4: 验证应用可启动**

```bash
cd /data/GJ/platform && python -c "from app import create_app; app = create_app(); print('app OK')"
```

Expected: `app OK`

- [ ] **Step 5: Commit**

```bash
git add routes/upload.py
git commit -m "feat: add check-10x and convert-10x API endpoints"
```

---

## Task 5: 前端改造

**Files:**
- Modify: `templates/upload.html`

- [ ] **Step 1: 在 `templates/upload.html` 的 `<div class="card p-4">` 内部、`upload-result` div 之后添加 10x 检测区域**

在 `</div>` (upload-result 的结束 div) 之后，`</div>` (card 的结束 div) 之前插入：

```html
            <div id="tenx-status" class="mt-3 d-none">
                <div class="alert alert-info">
                    <strong>10x Genomics 数据检测</strong>
                    <div id="tenx-files" class="mt-2 small"></div>
                    <div id="tenx-version" class="mt-1 small text-muted"></div>
                </div>
                <div class="card p-3 mt-2">
                    <div class="row mb-2">
                        <div class="col-md-6">
                            <label class="form-label small">物种（可选）</label>
                            <select class="form-select form-select-sm" id="tenx-species">
                                <option value="">不指定</option>
                                <option value="human">人类 (Human)</option>
                                <option value="mouse">小鼠 (Mouse)</option>
                                <option value="rat">大鼠 (Rat)</option>
                            </select>
                        </div>
                        <div class="col-md-6">
                            <label class="form-label small">基因组版本（可选）</label>
                            <input type="text" class="form-control form-control-sm" id="tenx-genome" placeholder="如 GRCh38、mm10">
                        </div>
                    </div>
                    <button class="btn btn-primary btn-sm w-100" id="btn-convert-10x" onclick="startConvert10x()">
                        转换为 h5ad
                    </button>
                </div>
            </div>
            <div id="tenx-convert-progress" class="mt-3 d-none">
                <div class="d-flex justify-content-between mb-1">
                    <span>10x 数据转换</span>
                    <span id="tenx-convert-pct">0%</span>
                </div>
                <div class="progress-container">
                    <div class="progress-bar-inner" id="tenx-convert-bar" style="width:0%"></div>
                </div>
                <div class="small text-muted mt-1" id="tenx-convert-msg"></div>
            </div>
            <div id="tenx-convert-result" class="mt-3 d-none">
                <div class="alert alert-success" id="tenx-result-msg"></div>
                <a href="/projects/{{ project.id }}" class="btn btn-outline-primary btn-sm">返回项目</a>
            </div>
```

- [ ] **Step 2: 替换 `<script>` 部分，支持多文件上传和 10x 检测**

将整个 `<script>...</script>` 块替换为以下内容：

```javascript
const zone = document.getElementById('upload-zone');
const fileInput = document.getElementById('file-input');
const progressDiv = document.getElementById('upload-progress');
const resultDiv = document.getElementById('upload-result');

zone.addEventListener('dragover', e => { e.preventDefault(); zone.classList.add('dragover'); });
zone.addEventListener('dragleave', () => zone.classList.remove('dragover'));
zone.addEventListener('drop', e => {
    e.preventDefault();
    zone.classList.remove('dragover');
    const files = e.dataTransfer.files;
    if (files.length) uploadFiles(Array.from(files));
});
fileInput.addEventListener('change', () => {
    if (fileInput.files.length) uploadFiles(Array.from(fileInput.files));
});

let uploadQueue = [];
let uploadIndex = 0;

function uploadFiles(files) {
    uploadQueue = files;
    uploadIndex = 0;
    uploadNext();
}

function uploadNext() {
    if (uploadIndex >= uploadQueue.length) {
        progressDiv.classList.add('d-none');
        resultDiv.classList.remove('d-none');
        document.getElementById('upload-msg').textContent = '所有文件上传完成！';
        check10x();
        return;
    }
    const file = uploadQueue[uploadIndex];
    uploadFile(file, () => {
        uploadIndex++;
        uploadNext();
    });
}

function uploadFile(file, onDone) {
    const formData = new FormData();
    formData.append('file', file);
    progressDiv.classList.remove('d-none');
    document.getElementById('upload-filename').textContent =
        `(${uploadIndex + 1}/${uploadQueue.length}) ${file.name}`;

    const xhr = new XMLHttpRequest();
    xhr.open('POST', '/projects/{{ project.id }}/upload');
    xhr.upload.onprogress = e => {
        if (e.lengthComputable) {
            const pct = Math.round(e.loaded / e.total * 100);
            document.getElementById('upload-bar').style.width = pct + '%';
            document.getElementById('upload-percent').textContent = pct + '%';
        }
    };
    xhr.onload = () => {
        if (xhr.status === 200 || xhr.status === 302) {
            onDone();
        } else {
            progressDiv.classList.add('d-none');
            resultDiv.classList.remove('d-none');
            document.getElementById('upload-msg').className = 'alert alert-danger';
            document.getElementById('upload-msg').textContent = '上传失败：' + file.name;
        }
    };
    xhr.onerror = () => {
        progressDiv.classList.add('d-none');
        resultDiv.classList.remove('d-none');
        document.getElementById('upload-msg').className = 'alert alert-danger';
        document.getElementById('upload-msg').textContent = '网络错误：' + file.name;
    };
    xhr.send(formData);
}

function check10x() {
    fetch('/projects/{{ project.id }}/upload/check-10x')
        .then(r => r.json())
        .then(data => {
            const statusDiv = document.getElementById('tenx-status');
            if (!data.has_10x) {
                statusDiv.classList.add('d-none');
                return;
            }
            statusDiv.classList.remove('d-none');
            const filesDiv = document.getElementById('tenx-files');
            const f = data.files;
            filesDiv.innerHTML =
                `<div>${f.barcodes} ✓</div>` +
                `<div>${f.genes} ✓</div>` +
                `<div>${f.matrix} ✓</div>`;
            document.getElementById('tenx-version').textContent =
                `版本: ${data.version === 'v3' ? 'v3 (features.tsv)' : 'v2 (genes.tsv)'}`;
        });
}

// 页面加载时也检测一次（处理已上传部分文件后刷新页面的情况）
check10x();

function startConvert10x() {
    const btn = document.getElementById('btn-convert-10x');
    btn.disabled = true;
    btn.textContent = '转换中...';

    const species = document.getElementById('tenx-species').value;
    const genome = document.getElementById('tenx-genome').value;

    const formData = new FormData();
    if (species) formData.append('species', species);
    if (genome) formData.append('genome', genome);

    fetch('/projects/{{ project.id }}/upload/convert-10x', {
        method: 'POST',
        body: formData,
    })
    .then(r => r.json())
    .then(data => {
        if (data.error) {
            btn.disabled = false;
            btn.textContent = '转换为 h5ad';
            alert('转换失败：' + data.error);
            return;
        }
        document.getElementById('tenx-convert-progress').classList.remove('d-none');
        pollConvertTask(data.task_id);
    })
    .catch(() => {
        btn.disabled = false;
        btn.textContent = '转换为 h5ad';
        alert('请求失败，请重试');
    });
}

function pollConvertTask(taskId) {
    const interval = setInterval(() => {
        fetch(`/api/tasks/${taskId}/status`)
            .then(r => r.json())
            .then(data => {
                document.getElementById('tenx-convert-bar').style.width = data.progress + '%';
                document.getElementById('tenx-convert-pct').textContent = data.progress + '%';
                document.getElementById('tenx-convert-msg').textContent = data.progress_message || '';

                if (data.status === 'completed') {
                    clearInterval(interval);
                    document.getElementById('tenx-convert-progress').classList.add('d-none');
                    document.getElementById('tenx-convert-result').classList.remove('d-none');
                    const summary = data.result || {};
                    document.getElementById('tenx-result-msg').textContent =
                        `转换完成！${summary.n_cells || '?'} 个细胞，${summary.n_genes || '?'} 个基因`;
                } else if (data.status === 'failed') {
                    clearInterval(interval);
                    document.getElementById('tenx-convert-progress').classList.add('d-none');
                    document.getElementById('tenx-convert-result').classList.remove('d-none');
                    document.getElementById('tenx-result-msg').className = 'alert alert-danger';
                    document.getElementById('tenx-result-msg').textContent =
                        '转换失败：' + (data.error_traceback || data.progress_message || '未知错误');
                }
            });
    }, 2000);
}
```

- [ ] **Step 3: 更新文件输入框的 accept 属性，包含 .mtx**

将现有的：
```html
<input type="file" id="file-input" class="d-none" accept=".h5ad,.h5,.csv,.txt,.gz,.xlsx,.xls" multiple>
```

替换为：
```html
<input type="file" id="file-input" class="d-none" accept=".h5ad,.h5,.csv,.txt,.gz,.xlsx,.xls,.mtx,.tsv" multiple>
```

- [ ] **Step 4: 更新上传区提示文字，说明支持 10x 格式**

将现有的：
```html
<p class="text-muted">支持格式：.h5ad, .h5 (10x), .mtx.gz, .csv, .txt, .xlsx, .xls</p>
```

替换为：
```html
<p class="text-muted">支持格式：.h5ad, .h5, .mtx/.tsv (10x Genomics), .csv, .txt, .xlsx, .xls</p>
```

- [ ] **Step 5: 手动验证应用可正常启动并访问上传页面**

```bash
cd /data/GJ/platform && timeout 5 python -c "
from app import create_app
app = create_app()
with app.test_client() as c:
    # 创建测试项目
    from models import Project
    p = Project(name='test')
    import os
    from config import Config
    proj_dir = os.path.join(Config.DATA_DIR, 'projects', p.id)
    for sub in ['uploads', 'intermediate', 'results', 'plots']:
        os.makedirs(os.path.join(proj_dir, sub), exist_ok=True)
    p.save()
    # 访问上传页面
    resp = c.get(f'/projects/{p.id}/upload')
    assert resp.status_code == 200
    assert 'tenx-status' in resp.data.decode()
    assert 'check-10x' in resp.data.decode()
    print('页面验证通过')
    # 清理
    import shutil
    shutil.rmtree(proj_dir)
    p.delete()
"
```

Expected: `页面验证通过`

- [ ] **Step 6: Commit**

```bash
git add templates/upload.html
git commit -m "feat: add 10x file detection UI and convert button to upload page"
```

---

## Task 6: 端到端验证

- [ ] **Step 1: 准备测试用 10x 数据文件**

```bash
cd /data/GJ/platform
mkdir -p /tmp/test_10x_data

# 用 Python 生成一组小规模测试 10x 文件（3 个细胞，5 个基因）
python3 << 'PYEOF'
import scipy.io
import scipy.sparse
import numpy as np
import os

out = '/tmp/test_10x_data'
# matrix.mtx (3 cells x 5 genes, sparse)
mat = scipy.sparse.csc_matrix(np.array([
    [1, 0, 3, 0, 5],
    [0, 2, 0, 4, 0],
    [6, 0, 0, 0, 7],
], dtype=np.float64).T)  # transpose: genes x cells
scipy.io.mmwrite(os.path.join(out, 'matrix.mtx'), mat)

# features.tsv (v3 格式)
with open(os.path.join(out, 'features.tsv'), 'w') as f:
    for i, (ens, sym) in enumerate([
        ('ENSG0000001', 'GENE_A'),
        ('ENSG0000002', 'GENE_B'),
        ('ENSG0000003', 'GENE_C'),
        ('ENSG0000004', 'GENE_D'),
        ('ENSG0000005', 'GENE_E'),
    ]):
        f.write(f'{ens}\t{sym}\tGene Expression\n')

# barcodes.tsv
with open(os.path.join(out, 'barcodes.tsv'), 'w') as f:
    for bc in ['AAAC_1', 'AAAC_2', 'AAAC_3']:
        f.write(bc + '\n')

print('测试文件已生成')
PYEOF
```

Expected: `测试文件已生成`

- [ ] **Step 2: 测试 `convert_10x_to_h5ad` 函数**

```bash
cd /data/GJ/platform
python3 << 'PYEOF'
import os
from modules.io_utils import convert_10x_to_h5ad

out = '/tmp/test_10x_output.h5ad'
adata = convert_10x_to_h5ad('/tmp/test_10x_data', out, species='human', genome='GRCh38')

assert adata.n_obs == 3, f"期望 3 个细胞，实际 {adata.n_obs}"
assert adata.n_vars == 5, f"期望 5 个基因，实际 {adata.n_vars}"
assert 'counts' in adata.layers, "缺少 layers['counts']"
assert 'gene_ids' in adata.var.columns, "缺少 var['gene_ids']"
assert adata.uns.get('species') == 'human', "species 未保留"
assert adata.uns.get('genome') == 'GRCh38', "genome 未保留"
assert os.path.exists(out), "h5ad 文件未生成"

import scanpy as sc
adata2 = sc.read_h5ad(out)
assert adata2.n_obs == 3
assert 'counts' in adata2.layers
print('convert_10x_to_h5ad 测试通过')

os.remove(out)
PYEOF
```

Expected: `convert_10x_to_h5ad 测试通过`

- [ ] **Step 3: 测试 check-10x API 端点**

```bash
cd /data/GJ/platform
python3 << 'PYEOF'
from app import create_app
from models import Project
from config import Config
import os, shutil

app = create_app()
with app.test_client() as c:
    p = Project(name='test-10x')
    proj_dir = os.path.join(Config.DATA_DIR, 'projects', p.id)
    uploads = os.path.join(proj_dir, 'uploads')
    os.makedirs(uploads, exist_ok=True)
    for sub in ['intermediate', 'results', 'plots']:
        os.makedirs(os.path.join(proj_dir, sub), exist_ok=True)
    p.save()

    # 无文件时
    resp = c.get(f'/projects/{p.id}/upload/check-10x')
    data = resp.get_json()
    assert data['has_10x'] == False

    # 复制测试文件
    import glob
    for f in glob.glob('/tmp/test_10x_data/*'):
        shutil.copy2(f, uploads)

    # 有文件时
    resp = c.get(f'/projects/{p.id}/upload/check-10x')
    data = resp.get_json()
    assert data['has_10x'] == True
    assert data['version'] == 'v3'
    assert 'features.tsv' in data['files']['genes']
    print('check-10x API 测试通过')

    shutil.rmtree(proj_dir)
    p.delete()
PYEOF
```

Expected: `check-10x API 测试通过`

- [ ] **Step 4: 测试 convert-10x 端到端任务流**

```bash
cd /data/GJ/platform
python3 << 'PYEOF'
import time, shutil, os, glob
from app import create_app
from models import Project, AnalysisTask
from config import Config

app = create_app()
with app.test_client() as c:
    p = Project(name='test-convert')
    proj_dir = os.path.join(Config.DATA_DIR, 'projects', p.id)
    uploads = os.path.join(proj_dir, 'uploads')
    os.makedirs(uploads, exist_ok=True)
    for sub in ['intermediate', 'results', 'plots']:
        os.makedirs(os.path.join(proj_dir, sub), exist_ok=True)
    p.save()

    # 复制测试文件
    for f in glob.glob('/tmp/test_10x_data/*'):
        shutil.copy2(f, uploads)

    # 触发转换
    resp = c.post(f'/projects/{p.id}/upload/convert-10x',
                  data={'species': 'human'})
    data = resp.get_json()
    assert 'task_id' in data
    task_id = data['task_id']

    # 等待完成（最多 30 秒）
    for _ in range(30):
        resp = c.get(f'/api/tasks/{task_id}/status')
        status = resp.get_json()
        if status['status'] in ('completed', 'failed'):
            break
        time.sleep(1)

    assert status['status'] == 'completed', f"任务状态: {status['status']}, error: {status.get('error_traceback', '')}"
    assert status['result']['n_cells'] == 3
    assert status['result']['n_genes'] == 5

    # 验证 h5ad 文件已生成
    h5ad_path = os.path.join(uploads, 'converted_10x.h5ad')
    assert os.path.exists(h5ad_path), "h5ad 文件未生成"
    print('convert-10x 端到端测试通过')

    shutil.rmtree(proj_dir)
    p.delete()
PYEOF
```

Expected: `convert-10x 端到端测试通过`

- [ ] **Step 5: 清理测试文件**

```bash
rm -rf /tmp/test_10x_data /tmp/test_10x_output.h5ad
```

- [ ] **Step 6: Commit（如果有修复）**

如果有任何修复，在此提交。否则跳过。

```bash
git add -A
git commit -m "fix: address issues found during end-to-end verification"
```
